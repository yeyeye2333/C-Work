#include"../other test/Foo.pb.h"
#include"chat_Socket.h"
#include<string>
int main()
{
    // Socket_listen lis;
    // lis._bind();
    // lis._listen();
    // int cli=lis._accept();
    // test::Foo a; 
    std::string *ts;
    std::string ss;
    ts=&ss;
    std::cout<<"?"<<ts->c_str()<<"hhh";
}