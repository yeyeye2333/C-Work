{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "g++",
            "args": [
                "-g",
                "${file}",
                "-std=c++20",
                "-o",
                "${fileBasenameNoExtension}.out"
            ],
            "presentation": { 
            "panel": "new" //默认为“shared“表示共享，改成new之后每个进程创建新的端口
            }
        }
    ],
    "version": "2.0.0"
}